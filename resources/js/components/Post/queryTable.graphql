query($page: Int!) {
    posts(page: $page) {
        data {
            id
            title
            country
        }

        paginatorInfo {
            currentPage
            perPage
            total
            lastPage
        }
    }
}
