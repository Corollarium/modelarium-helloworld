query($page: Int!) {
    users(page: $page) {
        data {
            id
        }

        paginatorInfo {
            currentPage
            perPage
            total
            lastPage
        }
    }
}
